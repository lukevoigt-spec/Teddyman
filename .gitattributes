# The pre-commit hook is a POSIX sh script — it MUST stay LF, or Windows autocrlf turns the shebang
# into "#!/bin/sh\r" and the hook fails with "bad interpreter" (#25 isolation guard would silently break).
.githooks/pre-commit text eol=lf
